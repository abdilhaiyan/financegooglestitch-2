/**
 * FamFinance Configuration
 *
 * 1. Copy this file to config.js:  cp js/config.example.js js/config.js
 * 2. Replace the placeholder below with your deployed Google Apps Script URL.
 * 3. config.js is gitignored — never commit your real URL.
 */

const FAMFINANCE_CONFIG = {
  // Paste your Apps Script Web App deployment URL here:
  endpoint: "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"
};
